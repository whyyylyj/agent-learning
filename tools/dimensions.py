# -*- coding: utf-8 -*-
"""两个实战案例的 10 维深挖（六步方法论版）+ 异常处理 case 集

每条字段：no/title/value/qm(问题定义)/fx(问题分析)/cj(问题场景)/fa(方案对比)/cq(采用与踩坑)/yz(验证与迭代)/wj(面试官深挖预演)/industry/insight
"""

DIM1 = [
dict(no=1, title="多知识库体系：按场景切分与路由",
 value="条款问答、佣金规则、产品手册、FAQ 的数据形态与更新频率完全不同——一个库装所有内容必然互相污染。",
 qm="症状是三类“串味”：条款问题混入 FAQ 口水话、佣金规则与产品手册互相干扰、rerank 之后依然串。把它定义成可度量的工程问题：检索返回 Top5 中含错误知识域片段的比例记为跨域污染率，灰度抽测约 18%【占位：__%】；进一步归因，错误答案里约四成能追溯到跨域污染。边界也要说清：这不是“检索排序不够好”——换更强 rerank 只降 3 个百分点，说明问题在库的结构而不在排序。",
 fx="排除法走三步：假设一 rerank 太弱，换强模型改善有限，排除主因；假设二分块太碎，单库内调整分块粒度无改善，排除；假设三 embedding 域适应差，换保险语料微调的向量模型略有改善但串味依旧。真正的根因：条款、FAQ、手册都是“保险话术”，语言风格天然相近，向量空间里本来就贴在一起——单库检索根本没有“域”的概念，它不知道自己不知道边界。",
 cj="典型现场：坐席问“XX 重疾的等待期是 90 天还是 180 天”，Top5 里 3 条条款 2 条 FAQ，FAQ 那句“90 天”是老产品口径——答案新旧口径混着说，坐席拿去答复客户就是事故。可复现：构造 20 道“条款 vs FAQ 口径冲突”探针题，单库模式下稳定复现串味。",
 fa=[("单库 + 域元数据过滤", "改动最小，一个索引管所有", "分块/嵌入参数只能全库折衷；索引重建互相影响；域仍是“逻辑分区”不是物理边界", "否"),
     ("多 collection 分库 + 意图路由", "每库独立分块策略（条款结构感知 / FAQ 一问一答）、独立索引参数；污染被物理隔离；可独立迭代与回归", "路由错一次答案就全错；collection 与运维面翻倍", "采用"),
     ("主域分库 + 小域合并（混合）", "折中，减少 collection 数", "小域之间又开始互相干扰，回到原点", "否")],
 cq="最终采用四库分立（条款 / 佣金规则 / 产品手册 / FAQ）+ 意图层输出目标库标签、检索前路由。踩坑一：路由错误是单点放大器——补上 Top2 库并行检索 + 融合排序，宁多勿错，串味率再降且召回回升约 6 个百分点【占位：__】。踩坑二：跨库问题（先查条款再核佣金规则）只能选一库——编排层支持两跳串联检索。踩坑三：FAQ 库高频重建索引引发线上抖动——改成蓝绿双索引原子切换。",
 yz="每库独立 golden set + 20 道跨库口径冲突探针集纳入回归；单库混装 vs 多库路由 A/B 对比：跨域污染率从约 18% 压到 2% 以下【占位：__%】。迭代机制：新知识接入走“数据形态 × 更新频率 × 权限级别”三判定 checklist，把“先塞进现有库再说”这种冲动挡在流程外。",
 wj=[("为什么不一个库加 metadata 过滤，省得路由？", "过滤解决的是查回来之后的权限与版本问题，解决不了分块与索引参数被全库平均的问题——条款要结构感知分块、FAQ 一条一问，同一套参数必然两头不讨好。分库本质是把参数自由度还给每个域，过滤和分库是两层正交的事。"),
     ("路由错了怎么办，错误的代价你接受吗？", "路由错误的代价被两层兜住：Top2 库并行检索保证召回不丢，rerank 融合排序保证错选的库排不进上下文。我们把路由错误从'答案错'降级成'排序问题'——这是可度量可优化的。"),
     ("库多了运维和回归成本呢？", "蓝绿切换解决重建抖动；每库独立 golden set 反而让回归更快——变更影响面从全库缩小到单库，回归集合小了、归因清楚了。")],
 industry="Morgan Stanley 内部知识库同样是“文档分类 + 权限域”先行；LangChain Router 是同类路由抽象。",
 insight="切分的本质不是分文件，是分“答案的可信边界”——每库有自己的证据标准与拒答口径。"),

dict(no=2, title="权限管控：检索前过滤与越权测试",
 value="佣金、客户数据涉及个人隐私与合规——权限是硬门槛，不是体验优化。",
 qm="问题的发现方式很不体面：测试坐席账号检索“续期佣金比例”，返回集里出现了浙江分公司的规则片段。精确定义为“检索可见性越权率”——无权限文档出现在返回集的比例，抽测非零即不合格。这是合规一票否决项：不是“优化项”，是“不上线项”。影响面评估：涉及跨机构规则文档、已下线产品、客户数据三类越权面。",
 fx="三层拆解后定位：接口级（谁能调这个能力）、库级（谁能查哪个库）、条级（谁能看哪条内容）。早期只做了接口鉴权，检索层是裸奔的——根本原因是检索系统天然只有“相关性”概念没有“可见性”概念，权限必须作为数据的属性注入而不是调用方的自觉。另一个被排除的假设是“入库时漏了机构字段”——字段在，但检索表达式根本没带过滤条件。",
 cj="现场还原：坐席 A（上海）问“浙江团队的续期佣金比例怎么算”，向量检索按语义召回浙江规则文档，生成层若无其事引用——答案技术上“正确”，权限上已经泄露。复现方式：低权限账号跑 50 条跨机构探针 query，统计越权返回。",
 fa=[("检索后过滤（先查再滤）", "实现简单，索引统一", "过滤前文档已进服务内存与日志，“查得到”本身就是泄露面；TopK 过滤后可能剩 0 条，把权限问题劣化为检索质量问题", "否"),
     ("检索前过滤（表达式强制注入）", "权限成为检索条件，无权限的数据物理不可见；与库级权限正交组合", "入库必须带全权限标签，标签错了就是事故；权限组合爆炸需控制粒度", "采用"),
     ("权限域物理分 collection", "隔离最彻底", "机构 × 角色 × 产品线的组合让 collection 数爆炸，不可运维", "否")],
 cq="采用：入库写入权限元数据（机构 / 角色 / 产品线）+ 框架层统一拼接权限过滤表达式——注意是框架层注入，业务代码不可绕过、也拿不到“不过滤”的接口。踩坑一：权限标签靠人工维护，组织架构一调整标签就过期——改为与 HR 系统定时同步 + 变更审计流水。踩坑二：静默过滤导致“明明该查到却查不到”，坐席困惑——意图层前置角色视角提示，检索不足时提示“当前权限范围未覆盖，可申请扩展”而不是空结果。踩坑三：权限过滤与召回率此消彼长——把“权限命中不足”作为独立信号上报，区分“真没有”和“看不到”。",
 yz="越权用例集（跨机构 / 跨角色 / 已下线内容）纳入发版回归，通过率必须 100%，任何一条失败阻断发版；季度红队渗透抽测。上线后越权事故为零【占位：__】。",
 wj=[("检索后过滤不行吗？反正用户也看不到？", "不行。过滤之前文档已经进入服务内存、日志、监控，“查得到”本身就是泄露；而且 TopK 过滤后可能剩零条，用户感知是“系统坏了”。检索前过滤让权限成为物理不可见，这两者的安全等级完全不同。"),
     ("权限标签本身错了怎么办？", "三重兜底：框架统一注入保证没有绕过路径、HR 系统同步保证标签新鲜、越权回归用例保证变更不引入漏洞。标签变更走审计流水，可回溯到人。")],
 industry="OpenAI × Morgan Stanley 案例：答案只允许来自内部内容——权限域与合规先行是金融 AI 共识。",
 insight="权限不是过滤器，是证据的属性——每条证据天生带权限标签。"),

dict(no=3, title="数据正确性校验：入库、版本与回流",
 value="知识库的错误是系统性错误——一条错误条款会污染所有相关问答。",
 qm="症状极具迷惑性：同一个问题昨天和今天答案不一样，像“灵异问题”。定义为入库质量缺陷率——抽检样本中解析错误（漏表格行 / 层级错 / 跨页拼接错）的比例；叠加版本混存缺陷——新旧条款同时在库，检索不区分生效日期。影响：错误是系统性的，一条坏数据污染所有命中它的问答，而向量检索让你无法预知“哪些问题会被命中”，排查半径极大。",
 fx="两类根因分开治：解析缺陷集中在三类（表格漏行约一半、标题层级树断裂、跨页表格拼接错位），是长尾但可聚类的；版本缺陷是流程缺失——入库只管“进”，没有“该谁生效”的概念。为什么难排查：症状出现在问答端，根因在数据端，中间隔了向量化与检索两层，坏例只能靠人工逆向追到源文档。排除的假设：“模型变笨了”——同一题复测答案漂移，且不同模型都漂，锁定数据侧。",
 cj="现场：2024 版条款 3 月 1 日生效，旧版未下架。3 月 5 日坐席问“等待期多久”，检索混回新旧两版片段，模型各引用一半。复现：构造 30 道“版本敏感问题”（新旧行为不同的条款点），稳定复现混引。",
 fa=[("人工全量校验", "最可靠", "条款文档上千份、每次更新都来一遍，成本不可行", "否"),
     ("解析置信度 + 人工抽检 + LLM 预审", "机器全量初筛、人只看低置信样本，成本与覆盖平衡", "置信度模型本身要调优；LLM 预审有误判，需要抽检对齐", "采用"),
     ("版本：新版本直接覆盖旧版", "实现最简单", "不可回溯、不可审计，监管检查一问就死", "否（改为双版本共存 + 生效日期过滤）")],
 cq="采用：解析侧三道关（表格行列完整性校验 / 标题层级树校验 / 跨页拼接检查）+ 5% 人工抽检；版本侧双版本共存、检索按当天日期过滤生效版本。踩坑一：解析校验规则越写越多成“规则地狱”——按错误类型聚类后聚焦 Top3 类型专项修复，覆盖八成缺陷，其余交给抽检回流。踩坑二：5% 抽检仍有漏网且人力贵——引入 LLM 预审（对解析结果做一致性自检，如“表格行列乘积与单元格数是否一致”），人工只审低置信样本，抽检成本降约七成【占位：__%】。踩坑三：版本过滤上线后旧问题“突然查不到”——按 query 时间与文档生效日期双过滤，并在答案里声明口径版本。",
 yz="解析错误率看板周更；30 道版本探针纳入回归；口径不一致投诉清零【占位：__】。迭代：错误样本自动回流解析器迭代队列，形成“线上坏例 → 数据缺陷 → 解析器修复 → 回归验证”闭环。",
 wj=[("为什么不直接上最强的文档解析模型？", "强解析解决不了版本与权限这类元数据问题——那不是“看不懂文档”，是“流程没有生效日期的概念”。而且解析错误是长尾分布，规则+置信度+抽检回流的性价比远高于换模型；模型再强也要这三道关兜底。"),
     ("怎么保证抽检本身不漏？", "抽检是兜底不是主力：机器做全量初筛（规则+LLM 预审），人只做低置信复核；线上坏例再逆向回流。三层任何一层接住都行，单层漏掉不致命。")],
 industry="保险条款的生效/废止管理在监管与行业内都有明确要求——版本化是合规刚需而非技术选择。",
 insight="正确性校验的哲学：机器做全量初筛，人做低置信复核——人力花在刀刃上。"),

dict(no=4, title="意图路由三层体系与阈值调优",
 value="意图是调度中枢：路由错，后面全错。",
 qm="把“感觉慢、感觉贵、说不清”翻译成三个可度量的问题：平均首响 2s+（目标 500ms 内）、月 token 成本高企【占位：__】、合规审计问“为什么这句被路由到办理类”时拿不出规则级依据。边界：这是单 LLM 分类方案的系统性问题，不是调 prompt 能救的——同一套方案调优两周，三项指标都没有数量级改善。",
 fx="流量分布分析给出关键洞察：Top5 意图占约七成流量【占位：__%】——大部分请求根本不配用 LLM 分类，是“用大炮打蚊子”。成本与延迟高度集中在低价值判断上。而可解释性问题的根因是：LLM 分类是黑盒概率，合规需要的是“命中了哪条规则”的确定性依据。三个问题指向同一个解：把请求分层，确定性高的走便宜层，不确定性留给 LLM。",
 cj="现场：早高峰坐席连续问高频问题（“XX 产品佣金多少”“等待期多久”），每个都过一遍 LLM 分类，首响堆到 2s+，坐席等不及直接关掉——转化率肉眼可见地掉。复现：压测脚本模拟 50 QPS 高频意图流量，单 LLM 层 P99 超 3s。",
 fa=[("全 LLM few-shot 分类（基线）", "实现快、新意图零成本", "延迟秒级、token 成本线性涨、不可解释", "否（保留为兜底层）"),
     ("规则 + LLM 两层", "高频意图毫秒级零成本", "中部意图（表述多变的高频类）规则覆盖不住，仍大量落入 LLM", "否"),
     ("规则 + 小模型 + LLM 三层", "频次 × 风险 × 可解释性三维最优；中部意图由微调小模型毫秒级接住", "小模型要标注与重训；置信度需要校准", "采用")],
 cq="采用三层：规则层（关键词+正则覆盖 Top 意图）、小模型层（微调分类器接中部）、LLM 兜底层（长尾与新增意图，few-shot + 意图定义文档，输出带置信度）。踩坑一：规则层写贪了——覆盖冲到 85% 后关键词开始互相冲突，误路由率抬头；回撤到七成覆盖，模糊区明确交给小模型。踩坑二：小模型置信度未校准——标称 0.9 置信的实际准确率只有 0.7，直接用原始分数设阈值全错；做温度缩放校准后分数才有意义。踩坑三：办理类等高风险意图误路由代价大——阈值按意图分级设置，高风险宁转人工，不追求统一的“最优阈值”。",
 yz="分层标注集（每意图 200+ 条）离线评测 + 线上 A/B 三维观测（误路由率 / 转人工率 / 首响延迟）。结果：意图 Top1 提升至【占位：__%】，平均首响从 2s 降到【占位：__】ms，月 token 成本降约八成【占位：__%】。迭代：新意图先由 LLM 兜底层顶住并落盘，积累 N 条自动触发小模型重训（半自动标注），形成“新意图冷启动 → 进化到便宜层”的流水线。",
 wj=[("为什么不直接用最强的大模型 few-shot 分类，一步到位？", "三个量都不答应：延迟（秒级 vs 毫秒级）、成本（七成流量不配用 LLM）、可解释性（合规要规则依据，LLM 给不了）。分层路由的精髓是把不确定性限制在最贵的那一层——便宜层消化确定性，这是成本工程的架构级解法。"),
     ("阈值怎么定的？拍脑袋吗？", "不是全局一个数：先做置信度校准（温度缩放让分数可信），再按意图的风险等级分别定阈值——办理类宁转人工，闲聊类宁放行。用混淆矩阵按季度重校，阈值是运营参数不是玄学。"),
     ("三层之间的边界会漂移吧？", "会，所以有“进化流水线”：LLM 兜底层落盘的新意图样本积累到阈值就触发小模型重训，边界是动态移动的，方向永远是“把流量从贵层往便宜层搬”。")],
 industry="阿里云 PAI 官方意图识别方案与多家保险客服实践都采用分层路由——“换大模型”在面经里是公认扣分项。",
 insight="分层路由的精髓：把“不确定性”限制在最贵的那一层，便宜层消化确定性。"),

dict(no=5, title="佣金问答：数字不过 LLM",
 value="佣金是销售最关心、最容易引发纠纷的数字——一分钱不能错。",
 qm="最初方案让 LLM 读佣金规则文档回答计算类问题，业务方评审时一票否决。把否决理由工程化：错误分三类——引用错规则（检索问题）、理解错规则（推理问题）、算错数字（计算问题）。前两类可以优化，第三类没有“可接受错误率”这个概念：佣金算错一分钱就是纠纷，算错一次就摧毁信任。问题定义：如何让数字类答案达到系统级精度（与源系统完全一致）。",
 fx="关键认知转折：数字答案的本质不是“生成”而是“查询”。佣金数据在源系统里是精确存在的——应收多少、实发多少、何时发放，都是确定性数据。让 LLM 去算，等于引入一个不必要的概率环节去回答一个本该确定的问题。被否掉的 NL2SQL 路线分析：权限（生成的 SQL 绕过应用层权限模型）、口径（SQL 里的统计口径无法受控）、审计（没人能审一段生成的 SQL）三关都过不了。",
 cj="现场：坐席问“我 12 月的保单什么时候开始返续期佣金”，模型把“第 13 个保单年度起”理解成“次年 12 月”——规则歧义 + 日期计算双重出错。复现：50 道计算类探针，错误率约 8%【占位：__%】，且错误形态分布三类都有。",
 fa=[("更强模型 + CoT 推理", "覆盖面广", "错误率只能压低不能归零；计算链越长错得越隐蔽", "否——底线不过"),
     ("NL2SQL 直查", "灵活", "权限 / 口径 / 审计三关全挂", "否"),
     ("槽位抽取 + 系统接口取数 + 模板拼装", "数字精度=源系统精度，天然带权限，全程可审计", "只覆盖模式化问题；长尾解释类问题要分流", "采用")],
 cq="采用四步链路：槽位抽取（险种 / 月份 / 人员身份，结构化输出）→ 权限校验（只能查本人或有权限的团队）→ 调佣金系统 API 取数 → 模板化拼装（数字原样透传，LLM 只做措辞润色且数字锁定不可改写）。踩坑一：槽位抽取不齐（月份、身份缺）时模型倾向猜默认值——改为澄清追问，宁可多一轮。踩坑二：接口 schema 变更导致模板渲染错位——接口返回带版本号，模板按版本渲染 + 契约测试。踩坑三：“为什么我的佣金是这么多”这类解释型问题被误判为计算型——意图再细分：计算类走 API，解释类走规则 RAG，两类链路分治。",
 yz="数字类专项评测：与佣金系统导出对账，要求 100% 一致（这是硬门槛不是统计指标）；越权用例回归；纠纷相关工单下降【占位：__%】。迭代：新模式化问题（如团体单佣金）按四步链路模板化扩展，成本以天计。",
 wj=[("让 LLM 算完再用规则校验一遍不行吗？", "校验的前提是有正确答案可对——正确答案从哪来？还得查源系统。既然查了，LLM 再算一遍就是引入一个不必要的风险源。把 LLM 从“计算者”降级为“转述者”，数字原样透传，风险类型直接消灭，这比“降低风险”高一个层级。"),
     ("这套只适合模式化问题吧，长尾怎么办？", "对，所以意图层先分诊：模式化的计算类走接口链路（占大头），解释类走规则 RAG，真正的长尾对话才给生成留空间。分诊错了怎么办——解释类问题里出现数字时同样走数字锁定透传，原则是全局的，不是链路局部的。")],
 industry="FinanceBench 实证 LLM 开卷金融 QA 错误率高；Morgan Stanley 案例（答案仅来自内部内容且经评估）支撑同一决策。",
 insight="数字不是生成问题，是查询问题——把 LLM 从“计算者”降级为“转述者”，风险就消灭了。"),

dict(no=6, title="保单变更：槽位抽取 + 规则引擎校验",
 value="变更办理是“合规 + 体验”双敏感场景：既要听懂自然语言，又不能让模型决定能不能办。",
 qm="早期直接让 LLM 判定“能否变更、需要什么材料”，出现两类严重问题：编造规则（“需在 3 天内办理”——规则里根本没有）与漏判限制条件（该险种受益人变更有范围限制）。定性：判定错误与佣金算错同级——每一次编造规则都是合规事故，没有错误率容忍空间。问题定义：让自然语言进入、让确定性判定出来，中间的翻译层如何设计。",
 fx="根因是职责错配：把“决定权”交给了概率模型。LLM 擅长的是把非结构化语言映射到结构化语义（抽取），不擅长的是承担确定性责任（判定）。抽取错误是可检测的（schema 校验不过、字段缺失可追问），判定错误是不可检测的（用户不知道规则是什么，无法发现模型在编）。把错误从不可检测区移到可检测区，是架构级的风险治理。",
 cj="现场：用户说“把受益人改成我女朋友”，模型回答“可以，请提供身份信息”——但该险种受益人范围规则要求未领证配偶需提供关系证明，且某些产品受益人变更有次数限制。模型不知道也没查，一次合规风险就这么产生。复现：30 道含隐藏限制条件的变更请求探针，纯 LLM 判定漏判率过半【占位：__%】。",
 fa=[("大模型 + 规则文档 RAG 判定", "实现快", "判定仍是概率行为，编造与漏判无法根除", "否"),
     ("LLM 抽取 + 规则引擎判定", "判定确定性 100%、可审计、规则可随监管更新", "需要把业务规则数字化（与业务共建规则库）；抽取层要兜错", "采用"),
     ("全人工办理", "零风险", "体验倒退回客服热线，项目价值归零", "否")],
 cq="采用：LLM 把自然语言抽成结构化变更请求（JSON schema 约束：险种 / 变更项 / 新值 / 关系），规则引擎按险种规则矩阵校验可行性并输出标准理由文本（附条款原文引用）。踩坑一：抽取 JSON 不合法或字段缺失——schema 校验失败后把错误信息回喂重试一次，再失败走澄清追问模板，绝不静默吞掉。踩坑二：与业务专家共建规则库时“规则数字化”丢语义——规则条目强制带条款原文，输出理由时引用原文而不是转述。踩坑三：多保单歧义（“我那份保单”命中多张）——反问让用户选，不猜；歧义处理是产品原则不是技术细节。",
 yz="抽取字段准确率人工标注对照【占位：__%】；规则判定与业务专家一致率 100%（规则引擎保证，抽检验证规则库本身）；端到端回归集覆盖每类变更规则。迭代：监管口径变化时只改规则库不发版——规则变更走评审，把合规更新的响应时间从周级压到天级。",
 wj=[("LLM 抽取也会错，凭什么信任它？", "关键不是“不出错”而是“错了能不能被发现”：抽取错误会被 schema 校验和澄清追问拦住（可检测），判定错误用户根本无从发现（不可检测）。架构上把风险从不可检测区挪到可检测区，这是比“提升准确率”更本质的治理。"),
     ("规则库维护成本谁出？", "规则库是业务资产，与业务方共建、每次监管口径变化走评审。这笔成本买的是“零编造规则”和审计合规——出一次合规事故的成本够维护规则库好几年。")],
 industry="金融行业通行做法：判定与执行走确定性系统，LLM 只做理解层——监管口径一致。",
 insight="LLM 负责“听懂”，规则负责“决定”——职责分离让两边都做各自最擅长的事。"),

dict(no=7, title="幻觉五层治理",
 value="幻觉是保险场景的合规生死线：一个编造的免责条款可能引发监管问题。",
 qm="上线初期最棘手的不是幻觉本身，是“幻觉率不可度量”——业务方反馈“有时对有时编”，无法定位是检索问题还是生成问题，优化无从下手。问题定义分两步：先建立度量（幻觉率按来源分类统计），再分层治理。不度量就治理等于蒙眼开炮。",
 fx="先归因再动手：抽样 100 条幻觉案例人工分类，四类来源——检索没召回、召回了但没排进上下文、上下文有但模型没引用、知识库里根本没有（硬答）。四类占比【占位：__%】直接决定治理优先级：检索类先修检索，知识缺失类先建拒答，生成倾向类才轮到调 prompt。这一步避免了大量无效的 prompt 调优。",
 cj="最危险的现场：坐席问“甲状腺结节 2 级能买 XX 重疾吗”，条款里没有明确除外表述，模型基于“医学常识”合成了一个除外条款——知识缺失型幻觉，语气笃定、看起来专业，杀伤力最大。复现：知识库外问题探针集，统计硬答率。",
 fa=[("只调 prompt（拒答指令 + few-shot）", "成本最低", "对所有来源一刀切，拒答率暴涨伤体验，且对检索类幻觉无效", "否（只作为生成层一环）"),
     ("五层布防：检索 / 生成 / 校验 / 产品 / 评测", "每层对一类来源设防，可度量可归因", "工程量大，需要持续运营", "采用"),
     ("换更强的模型", "省事", "强模型幻觉率低但不为零，且无法给出合规需要的确定性", "否")],
 cq="五层：检索层（混合检索 + rerank 提高证据质量）、生成层（grounded prompt + 拒答 few-shot 示例 + 低温度）、校验层（引用溯源——无源句不展示；数字类二次核对）、产品层（拒答即 feature：转人工 + 相似问题推荐）、评测层（幻觉专项集分类别度量）。踩坑一：只上生成层拒答指令时拒答率从 5% 涨到 15%，业务投诉“太保守”——阈值按意图与风险分层，闲聊类放宽、条款类从严。踩坑二：LLM-as-judge 与人工判断初期一致率仅七成——judge 用人工金标校准、错误样本回喂，一致率达标后才准进流程。踩坑三：数字幻觉与事实幻觉混在一个指标里失真——拆开：数字类零容忍走校验器，事实类走引用率统计。",
 yz="幻觉评测集（三类来源标注）自动回归 + 月度人工抽检对齐 judge；分类别幻觉率进看板：检索类 / 缺失类 / 生成类分别压至门槛以下【占位：__%】。迭代：每类来源的占比变化驱动下一轮投入方向——指标是导航不是装饰。",
 wj=[("幻觉能彻底消灭吗？", "不能，但可以变成可度量、可归因、可兜底的工程问题。我们把“感觉它编了”变成“哪一层漏了”——每类幻觉有 owner、有指标、有对策。面试里敢说“不能消灭”反而是加分的诚实，后面紧接治理框架就是亮点。"),
     ("拒答率高了业务方不满意怎么办？", "拒答是 feature 不是 bug——编一个像模像样的答案比拒答危险得多。关键是阈值分层（按风险等级）和拒答体验（转人工 + 相似问题推荐），把“拒答”从死胡同变成分流入口。")],
 industry="FinanceBench 实证 + Self-RAG/CRAG 提供检索自反思的学术路线；拒答设计是行业共识。",
 insight="幻觉治理的哲学：机器做全量防御，产品做残余兜底，度量贯穿始终。"),

dict(no=8, title="评测体系分层与“难找易验”",
 value="没有度量就没有优化：评测体系是整个平台最值钱的资产。",
 qm="症状：评测集跑分 90+，上线后多跳问题频频失败——“评测分数无法预测线上表现”。定义为分布错配问题：线上 badcase 里多跳、口径冲突、无答案类占比约六成【占位：__%】，评测集里这三类几乎为零。评测集考的是模型会的，不是用户问的。",
 fx="对线上 badcase 抽样归类发现分布长这样：单跳事实题（评测集全覆盖）只占四成，多跳对比题、版本口径题、无答案题才是大头——而这三类恰好是最难标注的，所以最初建集时被系统性回避了。根因不是偷懒，是“什么算对”在这三类题上没有书面定义：多跳题中间错了算不算对？口径冲突答新版算不算错？没答案该拒答还是该猜？——没有验收口径就没法标注。",
 cj="现场：回归全绿信心满满上线，坐席第一周就问出“XX 产品对比 YY 产品在轻症赔付上的差异”——两跳 + 表格对比 + 口径对齐，评测集里没有这一类，线上直接翻车。复现：把线上 badcase 按类映射回评测集，覆盖率一目了然。",
 fa=[("继续加题不分类", "数量好看", "分布错配依旧，分数继续骗人", "否"),
     ("难度 × 类别交叉分层扩容 + 证据链标注", "分数恢复预测力；坏例可归因到层", "标注成本高，验收口径要先与业务书面约定", "采用"),
     ("纯线上 A/B 代替离线评测", "最真实", "迭代慢、无法归因、坏体验直接伤害用户", "否（作为补充）")],
 cq="采用：按难度（单跳 / 多跳 / 无答案）× 类别（条款 / 佣金 / 变更）交叉分层扩容，每题标注证据链与验收口径；无答案类专项补充（对应 BFCL V3 的 irrelevance 维度——该拒的拒）。踩坑一：标注“什么算对”反复吵架——验收口径写进标注规范文档，业务方签认，争议 case 进仲裁池周会裁决。踩坑二：评测集膨胀后回归从 20 分钟涨到 2 小时——分层抽样回归：默认跑变更相关层全量 + 其余层抽样，全量回归留发版前。踩坑三：评测题泄漏进 prompt 迭代过程——评测集分 dev/holdout 双集，迭代只看 dev，holdout 只在里程碑跑。",
 yz="评测分与线上表现的相关性回测（这是评测集的有效性证明）；发版门禁从人审改为数据审（分层数据达标才放行）；线上坏例按层回流——评测集成为活资产。效果：评测集首次能预测线上表现，回归拦截率【占位：__%】。",
 wj=[("评测集多大才够？", "数量不如分布——分层覆盖（难度×类别×无答案）比堆量重要。BrowseComp 的“难找易验”原则：找难题的成本高，但验证答案便宜，出题预算花在刀刃上。"),
     ("LLM-as-judge 靠谱吗？", "是放大器不是真理：必须用人工金标校准一致率，错误样本回喂迭代。我们的 judge 校准到与人工九成一致才进流程，而且 judge 只用于抽检与回归，不用于发版门禁——门禁用确定性校验。")],
 industry="BrowseComp（OpenAI）与 BFCL V3（UC Berkeley）是评测设计的两个参照系：前者学难度设计，后者学维度拆分。",
 insight="评测集是公司的资产：它沉淀了“什么算对”的业务共识。"),

dict(no=9, title="冷启动与数据埋点：没有用户数据时怎么迭代",
 value="上线初期没有行为数据，优化无从下手——这是所有 toB AI 产品的共同困境。",
 qm="上线前两周的窘境：无点击数据、无负反馈，唯一信号是“客户没投诉”——而 toB 场景客户不投诉往往只是“懒得投诉”，弃用是静默的。问题定义：在自然行为数据积累起来之前，用什么信号驱动迭代优先级？空窗期持续越久，业务方耐心耗尽的风险越大。",
 fx="把信号来源拆成三档：能自动采集的（埋点事件：query、意图结果、检索命中、引用率、耗时）、能代理的（转人工率、复问率、会话放弃率——满意度的行为代理）、能主动要的（每周业务方评审会、灰度问卷）。关键认知：toB 的满意度不在点赞里，在“用户下次还来不来问”里——代理指标比直接反馈更真实。",
 cj="现场：业务方反馈“感觉不太准”，追问哪里不准、什么场景、哪类问题——说不上来。没有数据，改进方向只能靠猜，工程资源砸错方向两周都是常事。这个“感觉不准”后来靠埋点定位到：条款版本混存导致的口径漂移，占比三成【占位：__%】。",
 fa=[("等自然积累，先不动", "零成本", "空窗期两三个月，业务耐心耗尽，项目风险最高", "否"),
     ("全链路埋点 + 代理指标 + 主动评审", "空窗期也有信号；代理指标比问卷真实", "埋点设计要克制，否则噪音与成本失控", "采用"),
     ("灰度问卷直接问用户", "直接", "toB 用户敷衍填，回收率与真实性双低", "否（作为辅助）")],
 cq="采用：全链路事件埋点（从 query 进来到返回的每一步：意图结果、检索条数、rerank 分、生成耗时）+ 会话级点踩与原因勾选 + 每周“零引用答案清单”（无条款依据的回答人工复核）+ 业务方 badcase 评审会。踩坑一：埋点铺太全，日志成本与噪音齐涨——收敛到五类关键事件，其余按需加。踩坑二：点踩率极低（约 1%【占位：__%】）信号失真——引入复问率与放弃率交叉验证，单一信号不下结论。踩坑三：零引用清单人工复核量大——每周限量 + 按风险排序（涉数字、涉办理的优先）。",
 yz="上线一个月积累【占位：__】条真实 query；识别出【占位：__】类系统性问题进入迭代；埋点数据与人工抽样判断做一致性校验，代理指标与业务方主观评价做相关性回测——指标可信才有资格驱动迭代。",
 wj=[("没有标注数据怎么做评测？", "两条腿：探针集先行——先构造“我们必须答对”的题把底线守住；业务方评审会定性——真实分布慢慢进来。等数据攒够再建评测是最贵的选择，因为空窗期的方向错误没人纠正。"),
     ("点踩率那么低，埋点还有意义吗？", "有意义，但要换信号：toB 的真实负反馈是行为——复问（说明没答好）、放弃（说明没答到点）、转人工（说明不敢用）。点踩只是补充，行为代理才是主信号。")],
 industry="行业共识：智能客服解决率 90%+ 是标杆口径（平安/阳光），但“解决”的定义必须与业务方书面约定。",
 insight="toB 的满意度不在点击里，在“用户下次还来问”里。"),

dict(no=10, title="上下文超限：提示词的压缩与裁剪",
 value="多轮对话 + 多证据引用必然撑爆窗口：压缩策略直接决定成本与质量。",
 qm="症状出现在第 8~10 轮：上下文超限，早期用最粗暴的滑动窗口截断——把最早的对话整段丢掉。事故现场：用户第 2 轮说“投保人按我老婆的信息算”，第 9 轮再问时模型按投保人本人口径回答——约束被截断了。定义为“多轮约束保持率”问题：第 N 轮仍能正确引用前文约束的比例。",
 fx="根因是信息没有优先级模型：改口、承诺、身份声明这类约束性信息，和寒暄、过程性描述在截断策略眼里是平等的——先来先扔。而约束信息的价值密度恰恰最高且不可再生（丢了用户不会重说）。次要因素：检索证据按条数截断而不是按相关度，导致后续轮次引用质量漂移。",
 cj="复现场景：构造 20 组多轮会话（每组在第 2~3 轮埋入约束，第 8~12 轮验证），滑动窗口策略下约束丢失率过半【占位：__%】——用户视角就是“这 AI 失忆了”，而且是关键时刻失忆。",
 fa=[("滑动窗口截断", "实现零成本", "约束信息按时间被丢弃，关键信息恰恰在早期", "否"),
     ("全量摘要替换历史", "窗口可控", "摘要本身有损且贵；约束在摘要中被稀释——“改成…”这类关键改口最容易被摘掉", "否"),
     ("分级放置：状态区 + 滚动摘要 + 证据裁剪", "约束永不丢失；成本与质量平衡", "需要显式的信息分级逻辑，状态区要产品设计配合", "采用")],
 cq="采用五级分级：系统规则（永不动）> 当前任务状态（必须保）> 近期对话（全保）> 早期对话（滚动摘要）> 检索证据（按相关度裁剪）。两个关键设计：约束类信息（“我刚才说的不算，改成…”）显式写入状态区，不参与摘要；检索证据按 rerank 分数装箱而非按条数截断，并做引用句柄化。踩坑一：摘要把“过程”摘掉了但把“结论与承诺”也稀释了——摘要模板强制保留结论、承诺、改口三类要素。踩坑二：摘要本身耗时耗钱——阈值触发（超限才摘要）+ 增量摘要（只摘新增段）。踩坑三：分级逻辑一开始埋在 prompt 里不可见——状态区做成显式结构（产品层可见“当前确认的信息”），反而成为信任感来源。",
 yz="多轮一致性评测集（第 N 轮引用第 2 轮约束的通过率）+ 关键信息保留率人工评估；多轮约束保持率提升至【占位：__%】，单会话输入 token 下降约三成【占位：__%】。迭代：分级规则随 badcase 调整——每类丢失样本归因到“哪一级没接住”。",
 wj=[("为什么不直接上长上下文模型？", "长窗口解决“放得下”，不解决“记得住”——lost-in-the-middle 实证：塞得越多中间信息利用率越低；且成本随轮次线性涨。分层放置是结构解，扩窗口是容量解，结构解不依赖模型升级。"),
     ("状态区谁维护？模型自己写靠谱吗？", "约束识别用模型（它擅长），写入与失效用确定性逻辑（版本化、可审计）——又是“模型理解、规则决定”的同款分工。状态区对用户可见，错了会被发现，可检测性保住了。")],
 industry="LangGraph checkpoint / Manus 文件系统 / Mem0 记忆库是业界三种代表解法，对应“状态落库 / 句柄化 / 记忆蒸馏”。",
 insight="压缩不是丢信息，是把信息分层放置：约束进状态区、过程进摘要、证据进句柄。"),
]

DIM1_EXC = [
("工具调用失败（佣金接口 5xx/超时）","重试分级：接口超时 3s 重试 1 次（幂等查询）；仍失败则降级为'系统繁忙，已记录，稍后回复'并生成待办工单——绝不编造数字。业务上把'如实说拿不到'定义成正确行为。"),
("实体解析失败（用户说'把我那份保单改了'但命中多张保单）","不猜。反问澄清：列出候选保单让用户选择；单保单但变更项模糊时给'你想变更哪一项'菜单。原则：办理类动作宁可多一轮澄清。"),
("工具循环调用（A 工具结果触发再调 A）","状态机防环：同一工具同参数连续调用计数，超 2 次强制跳出并转人工/澄清；步数预算上限 12 步。"),
("LLM 输出不合法 JSON（抽取失败）","schema 校验失败 → 带错误信息重试一次（把校验错误回喂）→ 再失败走澄清追问模板；绝不静默吞掉，因为下游规则引擎需要合法入参。"),
("检索空结果","三级降级：①放宽（同义改写/去掉产品限定词）重检 ②跨库检索 ③仍空则明确拒答'知识库暂未覆盖'并转人工，同时记录为知识缺口需求。"),
("拒答被业务方投诉'太保守'","阈值分层与数据化：按意图与风险等级配不同阈值，每季度用人工标注校准'该答未答/不该答乱答'的混淆矩阵。"),
]

DIM2 = [
dict(no=1, title="场景识别与 skill 适配层：用户提问 → 技能路由",
 value="银行、保险、券商的用户提问形态各异，一个通用 agent 硬接所有问题，要么理解偏差要么漏分析步骤——skill 适配是绩效评价系统的第一道架构。",
 qm="灰度期的症状：同一句“这个组合最近表现怎么样”，银行理财经理要的是收益+回撤速览，券商投顾要的是归因到持仓的贡献分解——通用 agent 各给各的答案，都没有完整覆盖分析框架。定义为“场景-技能匹配率”问题：提问被路由到正确 skill 且分析框架完整执行的比例，初期抽测不足七成【占位：__%】。漏步骤比答错更隐蔽：数字都对，但归因没做、基准没比——结论表面成立实则残缺。",
 fx="关键洞察：绩效评价的问题空间是有限集。不像开放研究，基金组合分析的分析框架高度模式化——收益评价、风险评价、业绩归因、基准对比、持仓穿透、调仓评估等 N 类，每类有固定的组件编排套路。通用 agent 每次现场发挥等于每次重新发明分析流程，既不稳定也浪费。第二层分析：skill 粒度是关键变量——粒度太细（拆 20 个）路由错误率高且编排碎片化；太粗（一个大 skill）等于回到通用 agent。",
 cj="现场：“为什么这个组合二季度跑输基准 2 个点？”——正确路径是基准对比→收益分解→归因下钻→结论四步，涉及 3 个 skill 的编排。早期通用 agent 只做了收益对比就下结论“跑输因为跌幅大”——归因环节整个缺失，投顾拿到的是正确但无用的答案。复现：50 道框架完整性探针题，统计漏步骤率。",
 fa=[("单一通用 agent 端到端", "零路由成本，长尾灵活", "分析框架不保证完整；不可复现；token 成本高", "否"),
     ("意图分类直连固定 workflow", "确定性强、可复现", "刚性：问题稍有变化就接不住；新场景要改代码", "否"),
     ("skill 库 + 编排：场景识别 → skill 选择 → Plan/ReAct 编排", "框架完整性由 skill 保证；描述式路由零训练成本；skill 可独立迭代", "路由层要维护 skill 卡片；编排层复杂度上升", "采用")],
 cq="采用三层：场景识别（结合机构类型、提问语境、组合上下文）→ skill 适配（skill 卡片含场景描述 / 输入输出契约 / 前置条件 / 组件编排模板）→ Plan/ReAct 执行。踩坑一：初版 skill 拆成 20 个，路由错误率飙升——按分析框架聚合到 8 个，每个 skill 内部再参数化，路由错误率腰斩【占位：__%】。踩坑二：skill 描述文档就是路由的 prompt 上下文，描述模糊路由就漂——skill 卡片结构化重写（固定字段：适用场景 / 不适用场景 / 典型问法 / 输出物），路由稳定性显著提升。踩坑三：不该接的问题（“帮我预测明天涨跌”）被硬接——加入 irrelevance 检测（BFCL V3 同款思路），明确拒答并说明系统边界。",
 yz="场景-skill 匹配率标注集（每 skill 100+ 条）离线评测；框架完整性探针（每类 skill 的必做步骤清单）线上抽检；匹配率提升至【占位：__%】。迭代：新 skill 上线走“LLM 兜底先行 → 样本积累 → 固化为 skill 卡片”的路径，与案例一意图分层的进化流水线同构。",
 wj=[("为什么不微调一个场景分类模型？", "skill 集合会增删——描述式路由（skill 卡片即上下文）零训练成本随改随生效；微调模型每次 skill 变更都要重训。等路由样本积累够多，把高频场景下沉为便宜层是顺理成章的优化，架构上预留了这条路。"),
     ("skill 和 function calling 有什么区别？", "function 是单次工具调用，skill 是分析框架——一个 skill 内部编排了多个组件的调用顺序、中间结果传递和校验规则。skill 是“食谱”，工具是“食材”，这层抽象让 agent 从“会用工具”升级到“懂分析方法论”。")],
 industry="东方财富妙想的 Skills 能力体系、Dify/Coze 的技能化编排是同一趋势——分析型 agent 正从“万能对话”转向“技能编排”。",
 insight="领域 agent 的护城不是通用智力，是把分析方法论固化为 skill 的领域工程。"),

dict(no=2, title="数据层复用：不建新数据源，复用已有资产",
 value="绩效评价系统底下的数据层（基金净值、持仓、组合、基准、因子）原系统早就有了——agent 层的任务是复用而不是重建。",
 qm="架构评审时的核心决策：agent 要访问的数据（净值序列、持仓明细、组合构成、基准曲线、因子暴露）在原系统数据层全部存在且经过多年校验。问题定义：复用与重建的边界在哪里？量化背景：两套数据层的口径分裂是致命风险——用户拿 agent 结论与原系统页面对照，出现“系统里 7.2%、agent 说 7.1%”就是信任崩塌事故，而重建数据管道产生口径漂移的概率接近必然。",
 fx="逐项分析数据需求：agent 需要的数据按“读取模式”分三类——批量时序拉取（净值、收益率序列）、明细查询（持仓、交易）、聚合计算（区间收益、风险指标）。前两类原数据服务 API 直接覆盖；第三类属于计算不属于数据——是能力组件的事（见维度 3）。真正的缺口只有一个：原 API 为页面交互设计，单次粒度小，agent 一次评价要串几十次调用——这是接入层要解决的工程问题，不是重建数据层的理由。",
 cj="现场：绩效评价要算某组合近三年逐月收益+回撤+相对基准偏离，按原页面 API 逐月拉取要 36 次调用，单次任务耗时分钟级且容易被限流。而数据层其实有批量端点的可能性——这是接入模式问题不是数据缺失问题。",
 fa=[("agent 直连数仓写 SQL", "灵活", "权限绕过应用层、口径无治理、SQL 不可审计——三关全挂（NL2SQL 同款死因）", "否"),
     ("复用已有数据服务 API + 协商补批量端点", "口径与源系统天然一致；权限模型继承；审计可行", "要说服数据团队为 agent 场景改造；页面粒度 API 需要适配层", "采用"),
     ("为 agent 新建专用数据视图层", "读写隔离，定制自由", "两套口径的漂移风险永久化；双倍维护成本", "否")],
 cq="采用：复用原数据服务 API 为唯一数据入口，权限透传（机构 / 用户 / 组合范围的可见性原样继承）；与数据团队协商新增批量时序端点（一次拉区间序列）。踩坑一：agent 流量与生产页面流量共享数据层配额——早高峰评价任务与理财经理打开页面抢配额，页面卡顿被投诉——为 agent 通道独立配额 + 错峰执行批量任务。踩坑二：部分老接口无字段级口径说明（“收益”是费前费后不明确）——拉通数据团队补口径字典，接口元数据带口径标签。踩坑三：接口超时按页面标准（30s）设置，agent 多步编排里一步挂全链挂——接入层统一收敛超时并分级。",
 yz="数字一致性基线：agent 链路产出的核心指标与原系统页面抽样对账，一致率必须 100%（口径标签对齐后）；数据层零事故【占位：__】。迭代：批量端点按 agent 使用频率逐步扩容，形成“高频访问模式 → 沉淀为专用端点”的数据服务演进循环。",
 wj=[("复用老系统 API 会不会被老系统的性能拖累？", "会，所以做了三件事：批量端点消解调用次数、独立配额避免与生产抢资源、超时分级让 agent 步骤快速失败。复用的收益是口径一致性和权限继承——这两个用钱买不到，性能问题反而有标准解法。"),
     ("为什么坚决不让 agent 写 SQL 查数仓？", "权限（生成的 SQL 绕过应用层权限模型）、口径（统计口径无法受控）、审计（没人能审一段生成的 SQL）三关都过不了。金融场景里这三个都不是技术偏好是监管要求。")],
 industry="Wind MCP、iFinD MCP 的行业方向就是“数据能力接口化开放”——数据层复用 + 接口化封装与整个行业趋势同构。",
 insight="agent 项目的第一架构决策往往不是“用什么模型”，是“复用什么、新建什么”——口径一致性比任何智能都值钱。"),

dict(no=3, title="能力组件的 agent 化封装：内部组件 → 工具 API",
 value="原系统后面早就沉淀了绩效计算、风险指标、归因分析等能力组件——把它们封装成 agent 可调用的工具，是整个系统的核心工程。",
 qm="现状盘点：收益计算、波动率/回撤/夏普等风险指标、Brinson 归因、风格分析——这些组件以内部 RPC 形式存在，与页面逻辑耦合（入参依赖页面会话上下文、返回直接给前端渲染）。问题定义：如何把“给人用的组件”改造成“给 agent 用的工具”？判断标准三条：参数可从自然语言推导（schema 化）、调用可重试（幂等）、错误可理解（语义化）。",
 fx="逐条对照三条标准找差距：入参方面，组件要的会话上下文（产品线、当前组合 ID）agent 拿得到但要显式传，隐藏依赖必须显式化；幂等方面，查询类组件天然幂等，但要防止 agent 重试时重复计算浪费配额；错误方面，组件错误是内部错误码（“ERR-2043”），agent 无法据此决策重试还是换路——错误必须翻译成行为语义（可重试 / 需换参 / 需人工）。还有一个容易被忽视的维度：工具描述质量直接决定 ReAct 的工具选择准确率——描述是给模型看的 API 文档。",
 cj="现场：agent 要算“组合二季度的 Brinson 归因”，工具选择正确但入参传了组合代码而非内部持仓快照 ID——组件静默返回了错误口径的结果（用最新持仓回溯历史归因）。这类“参数对了但语义错了”的失败最隐蔽：数字看起来完全合理。对策：工具入参做语义校验（时间点敏感参数必须显式传快照语义）。",
 fa=[("直接暴露内部 RPC", "零改造成本", "会话耦合、错误码不可理解、无 schema——agent 调用错误率高且不可归因", "否"),
     ("建 agent 工具网关统一封装", "schema 标准化、错误语义化、鉴权限流统一、工具目录集中管理", "网关是新增组件要维护；封装层要跟组件版本对齐", "采用"),
     ("按 agent 场景重写组件", "最贴合", "重复造轮子，丢掉原系统多年校验的正确性积累", "否")],
 cq="采用 agent 工具网关：每个组件封装为工具卡片（名称 / 功能描述 / 入参 schema 含语义约束 / 出参结构 / 错误语义 / 典型调用示例）。踩坑一：工具描述最初由后端随手写，ReAct 工具选择错误率高——描述重写为“给模型看的文档”：典型问法、参数语义、边界条件，选择准确率大幅回升【占位：__%】。踩坑二：枚举型入参（组合 ID、基准 ID）模型经常幻觉编造——枚举值从数据层动态拉取注入候选列表，模型只能选不能编。踩坑三：工具数量增长后选择准确率衰减（工具越多干扰越大）——按 skill 分组限定可见工具集，每个 skill 只看得到自己的工具，选择空间收窄。",
 yz="工具调用成功率（选择正确 × 参数合法 × 结果可用）分 skill 统计；参数幻觉率专项（枚举字段编造率必须为零——靠动态候选机制结构性保证）；错误分类统计驱动组件改造优先级。效果：工具调用一次成功率【占位：__%】，枚举幻觉结构性归零。",
 wj=[("工具描述不就是个文档吗，值得当设计重点？", "对 agent 来说描述就是 API 的全部——模型没看过你的代码，它对工具的全部认知来自描述。我们重写描述后工具选择准确率的提升，比换一个更强的模型还大。这是“提示工程在工具层的延伸”，很多人在这里失分。"),
     ("怎么防止模型编造参数？", "结构性防御：枚举类参数不给自由文本空间——候选值动态拉取、模型只能选不能编。参数幻觉靠 prompt 约束是压不干净的，靠机制才能归零。")],
 industry="MCP 的工具描述规范、各家 function calling 最佳实践都强调 schema 语义质量——工具工程正成为独立学科。",
 insight="工具封装的本质是翻译：把“给系统的接口”翻译成“给模型的接口”——schema 是语法，描述与错误语义是语用。"),

dict(no=4, title="Plan + ReAct 混合推理：任务 DAG 骨架 + 节点内 ReAct",
 value="纯 ReAct 一步步走会漂移，纯 Plan 一次规划定死无法应对执行反馈——绩效评价这种多步任务需要两者杂交。",
 qm="两类失败模式实测：纯 ReAct 模式下，长任务（十几步组件调用）出现方向漂移与循环——某次归因异常后 agent 反复重算同一指标五步不出结果；纯 Plan 模式下，规划一次定死，执行中发现数据缺失（某月持仓快照不全）无法补位，整单失败。定义为“长程任务的稳定性”问题：完成率、步数效率（实际步数/必要步数）、循环发生率三个指标刻画。",
 fx="根因分析：ReAct 的本质缺陷是局部性——每步决策只看上一步观察，缺乏全局任务视图，长链路误差累积必然漂移；Plan 的本质缺陷是刚性——规划时依赖的信息在执行中会失效（数据缺失、指标异常），无反馈通道。两者恰好互补：Plan 提供全局骨架（防漂移），ReAct 提供局部灵活（应对意外）。关键设计点：重规划的触发条件与预算——无限重规划等于失控，零重规划等于刚性。",
 cj="现场：“对比组合 A 和 B 上半年的风险收益特征”——规划出 12 节点 DAG（各自指标计算 + 对比 + 结论），执行到 B 的波动率节点时数据缺失。纯 Plan 直接失败；纯 ReAct 早就漂了；混合模式：该节点触发受限重规划，跳过缺失项并标注“该指标数据缺口”，其余节点继续，最终交付带缺口声明的结论。",
 fa=[("纯 ReAct（思考-行动-观察循环）", "灵活、实现简单", "长任务漂移与循环；步数预算失控；不可复现", "否（保留在节点内部）"),
     ("纯 Plan-and-Execute", "全局视图清晰、可复现", "刚性：执行期无反馈修正，数据缺失即全盘失败", "否"),
     ("Plan 骨架 + 节点内 ReAct + 受限重规划", "骨架防漂移、节点内灵活、重规划带预算", "编排器复杂度高；状态传递设计要小心", "采用")],
 cq="采用：先 Plan 生成任务 DAG（节点=组件调用或子分析，边=数据依赖），节点内 ReAct 处理参数准备与异常，节点间传递结构化中间结果。受限重规划：最多 2 次，且需满足触发条件（关键节点失败 / 覆盖率不足），重规划只调整受影响子图。踩坑一：中间结果全塞上下文，几步之后窗口爆炸且注意力稀释——状态外置为“任务黑板”（见维度 7），上下文只放当前节点所需。踩坑二：循环调用（同工具同参数反复调）——状态机防环：连续同参调用计数超 2 次强制跳出转重规划评估。踩坑三：重规划的触发阈值定太敏感，小异常也触发全局重规划，成本翻倍——区分“节点级失败”（局部处理）与“任务级失败”（才触发重规划）。",
 yz="完成率、步数效率、循环发生率三指标进看板；完成率从纯 ReAct 的【占位：__%】提升至【占位：__%】，循环调用基本消除；同一问题多次执行的结果稳定性（可复现性）显著改善。迭代：重规划案例库积累——每次重规划的触发原因与处理方式落盘，反哺规划器的 few-shot 示例。",
 wj=[("为什么不干脆端到端 RL 训一个策略？", "DeepResearcher 证明端到端 RL 可行且效果更好，但它需要环境与奖励基建。工程节奏：编排先拿业务结果、沉淀任务轨迹与评价，这些轨迹就是未来 RL 的训练数据、评测就是奖励函数——两步走不冲突，是先后手。"),
     ("Plan 和 ReAct 的边界怎么划？", "按“不确定性在哪”划：分析框架（做什么、什么顺序）是确定性的，Plan 管；执行细节（参数怎么凑、异常怎么处理）是不确定的，ReAct 管。确定性上收、不确定性下放——和案例一意图路由“确定性走便宜层”是同一个设计哲学。")],
 industry="Plan-and-Execute 与 LMCompiler（并行函数调用）是学术参照；Anthropic 编排者-执行者实践互相印证。",
 insight="骨架防漂移，肌肉防僵硬——长程任务的稳定性来自两种推理模式的杂交，而不是二选一。"),

dict(no=5, title="并行计算编排：多指标并行调用组件 API",
 value="一次绩效评价要调 20+ 次组件（多指标 × 多组合 × 多区间），串行分钟级用户等不了，并发拉满会把共享数据层打挂——并行编排是工程主战场。",
 qm="量化问题：串行执行单次评价 3~5 分钟【占位：__】，用户预期 30 秒内给分析；而天真地把并发开满（全组件同时打），数据层 QPS 冲击引发限流，连累同租户的生产页面。定义双约束：时延目标（30s 内完成计算编排）+ 礼貌约束（agent 流量不得影响生产流量）——后者是复用内部数据层的特殊代价。",
 fx="并行化的结构分析：组件调用间存在依赖图——收益指标之间独立（可并行），归因依赖收益结果（必须后置），对比依赖双方各自完成（汇合点）。并行度上限由两件事决定：无依赖节点的宽度（约 8~12【占位：__】）和数据层每端点的配额。真正要设计的是“依赖感知调度 + 全局并发闸门 + 失败语义”三件套，其中失败语义最容易被忽视：部分组件失败时，整单失败？降级交付？等待重试？——不同指标的重要性不同，答案不该一刀切。",
 cj="现场：早盘集中来了 5 个评价任务，每个 30+ 组件调用，并发全开——数据层某核心端点 QPS 超限，触发限流，连理财经理的页面查询都开始超时。这次事故确立了“agent 流量必须让路生产流量”的铁律。",
 fa=[("全串行", "对上游零压力，实现简单", "分钟级时延，用户体验不可接受", "否"),
     ("全并行 + 全局线程池", "时延最优", "打挂数据层、连累生产页面；失败语义混乱", "否"),
     ("依赖感知 DAG 并行 + 每端点独立限流 + 分级降级", "时延与上游友好的平衡点；失败有语义", "调度器复杂；配额要持续调优", "采用")],
 cq="采用：调度器按 DAG 拓扑就绪即调度（无依赖节点并行执行），每端点独立令牌桶限流（配额与数据团队书面约定），核心指标失败重试（幂等）+ 熔断，非核心指标失败标注缺口继续。踩坑一：超时一刀切导致快端点被拖、慢端点被掐——超时分级（快端点 5s / 重计算端点 15s），并按 P90 实测持续校准。踩坑二：重试风暴——某端点抖动时多任务同时重试放大压力——指数退避 + 熔断半开探测。踩坑三：部分失败时报告“静默缺项”（用户不知道少了指标）——缺口显式化：报告里明确标注“该指标因数据缺口未计算”，把降级变成透明的信息而不是隐藏的缺陷。",
 yz="P50/P99 编排时延进看板；单次评价从【占位：__】分钟降到【占位：__】秒；数据层因 agent 流量的限流事件归零（配额看板双向可见）；缺口标注率 100%（凡是缺项必声明）。混沌演练：定期人为掐断单端点验证降级路径。",
 wj=[("并发度怎么定的？", "三层决定：DAG 无依赖宽度决定理论上限、每端点配额决定实际上限、成本预算决定意愿上限——三者取最小。而且配额不是抢来的是和数据团队协商的，agent 流量让路生产流量是复用内部数据层的铁律，这背后的判断是：系统整体的可用性高于单次任务的时延。"),
     ("这和一般的微服务调用编排有什么区别？", "骨架是同一套（依赖感知调度、舱壁隔离、熔断降级——分布式基本功的直接迁移，我的异步轮询框架经验全用上了），新问题在失败语义的“业务化”：传统编排失败就是失败，agent 编排要区分“这个指标缺了报告还成不成立”——失败语义成了业务判断，这是 agent 工程给分布式编排带来的新维度。")],
 industry="bulkhead 舱壁隔离与超时分级是微服务成熟实践——agent 的多路调用本质是分布式 IO 编排，功底直接迁移。",
 insight="降级不是失败，是“诚实地告诉用户我知道多少”——缺口显式化比静默残缺专业一个量级。"),

dict(no=6, title="结果勾稽与数字校验：agent 结论必须与原系统对得上账",
 value="用户会拿 agent 的结论与原系统页面互查——任何一处不一致都是信任事故。绩效评价的数字红线：勾稽一致。",
 qm="定义“勾稽一致”：agent 输出的每一个数字，与其数据来源（组件返回 / 数据层取值）逐位一致，且口径（费前费后、日历年 vs 交易日、万元 vs 亿元）显式声明。灰度期抽测发现三类不一致：转写错误（模型抄错——“120.3 亿”写成“102.3 亿”）、口径漂移（agent 用费前收益对比费后基准）、精度不一致（四舍五入规则不同导致尾差）。任何一类都足以让用户否定整个系统。",
 fx="三类根因分开治：转写错误的本质是“数字经过了生成环节”——模型在压缩、改写、组织语言时对数字做的是概率性复制；口径漂移的本质是元数据缺失——数字离开组件时没带走自己的口径标签；精度不一致是工程细节——舍入规则没有全局约定。第一类是结构性风险：只要数字过 LLM 笔头就有抄错概率，治本方案是数字不过 LLM（与案例一佣金链路同款原则的复用）。",
 cj="现场：投顾把 agent 生成的评价报告里的“组合年化收益 7.21%”截图给客户，客户自己打开系统看到 7.2%——尾差（agent 舍入到两位、系统一位）。客户问“到底哪个对”，信任瞬间归零。从此确立：舍入规则全局统一，且报告数字必须与页面显示规则一致。",
 fa=[("生成后人工逐数核对", "可靠", "一次报告几十个数字，人力不可持续", "否"),
     ("claim 级勾稽：报告拆原子命题逐条对账 + 口径标签强制", "自动化可持续；错误可定位到环节", "要建 claim 拆解与数字回链的基建", "采用"),
     ("全模板拼装（数字不进生成）", "结构性根除转写错误", "分析结论的组织与表述灵活性大幅下降", "部分采用（数字段模板化，叙述段生成）")],
 cq="采用混合：报告拆成原子 claim，数字类 claim 强制走模板段（数字原样透传不过 LLM 改写），叙述类 claim 生成后逐条勾稽对账。组件返回强制带口径元数据（费前/费后、时间窗类型、单位、舍入规则），报告展示口径标签。踩坑一：claim 拆解粒度定不好——太粗对不上账、太细成本高——按“一个数字一个 claim”的最小原则落地。踩坑二：勾稽误报打断生成流程——误报样本回流校验器迭代阈值，区分“真不一致（打回）”与“格式差异（放过）”。踩坑三：口径标签一开始只有单位——补全时间窗语义（“近一年”是自然年还是滚动 365 天必须显式）。",
 yz="数字勾稽一致率（agent 报告数字 vs 组件返回原文）纳入每次回归，目标 100%；口径标签覆盖率 100%；转写错误率结构性归零（数字段不过 LLM）；用户报告的数字质疑工单清零【占位：__】。",
 wj=[("为什么数字经过 LLM 就一定会错？", "不是一定，是概率——而金融场景对数字的错误容忍是零。生成模型对数字做的是概率性复制，链路越长抄错概率越高。结构性解法就是不让它抄：数字段模板化透传，叙述段再灵活。这与我在保险佣金链路用的是同一条原则——数字不过 LLM，跨场景复用的架构原则本身就是亮点。"),
     ("口径问题怎么彻底防住？", "元数据前移：数字在组件返回时就带全口径标签（费前费后/时间窗/单位/舍入），报告只是透传标签。口径不是生成时“注意”的事，是数据结构里自带的事。")],
 industry="金融审计的“数字勾稽”思想与学术 claim verification 同构——每个数字可回溯是共同底线。",
 insight="勾稽一致是 agent 结论进入金融决策链的准入证——宁可少说，不可说错。"),

dict(no=7, title="多步推理的中间结果治理：任务黑板与状态外置",
 value="十几步组件调用产生的中间结果（指标表、持仓快照、归因中间量）全塞上下文，几步就撑爆窗口且注意力稀释——状态外置是必选项。",
 qm="症状：任务进行到 8~10 步，上下文里堆着前面所有组件的完整返回（每段几百到几千 token），出现两类问题：窗口超限直接报错；更隐蔽的——没超限但模型开始“看不见”早期的关键数字（长上下文注意力稀释），后续计算引用错对象。定义为“中间结果的生命周期管理”问题：每个中间结果应该在什么阶段以什么形态存在于哪里。",
 fx="分析中间结果的访问模式：绝大多数中间结果只在“产生它的节点”和“消费它的节点”被需要（局部性），只有少数（关键结论、全局约束、口径标签）需要全程可见（全局性）。这个访问模式直接给出解法：局部结果外置存储（按句柄引用），全局信息进小型黑板。类比操作系统：内存放活跃数据，磁盘放全量数据，页表做映射——黑板就是页表。",
 cj="现场：归因分析节点需要“区间收益”节点的结果，但那个结果在 6 步之前、上下文 1.2 万 token 之前——模型引用了另一个组合的收益数字（两个组合的中间结果长得几乎一样）。这类“张冠李戴”错误在中间结果堆积后频发，且肉眼难察。",
 fa=[("全量中间结果进上下文", "模型看得最全", "窗口爆炸；注意力稀释引发张冠李戴", "否"),
     ("全量外置 + 需要时检索回填", "窗口友好", "检索回填有延迟与错取风险；数字类结果检索错代价高", "否（作为补充）"),
     ("分级：数字类句柄化 + 结论进黑板 + 过程性结果外置", "关键信息常驻、按需取用、窗口可控", "分级逻辑与读写协议要精心设计", "采用")],
 cq="采用任务黑板：全局区（任务目标、全局约束、口径标签、关键结论卡——常驻小体积）+ 局部区（各节点中间结果，句柄化存储按需拉取）。数字类中间结果永不摘要、只句柄化（数字一经摘要就有失真风险）。踩坑一：黑板写入协议不清，多个并行节点并发写冲突——黑板分区加版本号，写入走追加不改写（append-only），冲突以最新版本为准并留痕。踩坑二：句柄拉回时的“取错”问题——句柄带语义描述（节点名 + 数据内容摘要），拉取前先验描述匹配。踩坑三：黑板本身越写越大（结论卡累积）——结论卡也分级：当前节段的结论常驻，历史节段结论合并为高层摘要（但数字仍然原文保留在句柄层）。",
 yz="长任务（15+ 步）完成率提升【占位：__%】；中间结果引用错误（张冠李戴）在探针集中归零；单任务上下文峰值下降约六成【占位：__%】。迭代：黑板的“哪些信息该常驻”规则随 badcase 持续调优——本质是在为这个领域学习“注意力该往哪放”。",
 wj=[("这和 LangGraph 的 checkpoint 有什么关系？", "同族思想不同层次：checkpoint 解决“断点恢复”（持久化），黑板解决“注意力分配”（什么该被模型看见）。我们的黑板在 checkpoint 之上加了领域分级——数字句柄化、结论卡常驻——这是通用框架没给的，要对业务错误做归因后才设计得出来。"),
     ("数字为什么不摘要？", "数字摘要=概率性改写，3.14159 摘成“约 3.14”在叙述里没问题，在勾稽链里就是错。我们对中间结果的态度和报告层一致：数字只有原文形态，要么全文要么句柄，没有第三种。")],
 industry="Manus 的文件系统当上下文（句柄化）与 LangGraph checkpoint（状态外置）是业界两条代表路线，黑板是两者与领域规则的杂交。",
 insight="上下文管理的本质不是塞多少，是让模型在每一步看见“该看的”——黑板是给注意力做的操作系统。"),

dict(no=8, title="上下文超限的压缩与裁剪：证据与历史的分层预算",
 value="多组件返回累积远超窗口：压缩策略直接决定成本与质量——这一维与案例一的压缩一脉相承但对象不同（指标表 vs 对话历史）。",
 qm="对象变了：案例一压缩的是“对话历史 + 检索证据”，这里压缩的是“组件返回的指标表 + 中间结论 + 任务指令”。量化：一次完整评价的原始中间结果总量 3~8 万 token【占位：__】，可用窗口预算 1.2 万——压缩比要求 3~6 倍，且数字不能失真、口径不能丢、约束不能摘掉。",
 fx="按“可压缩性”给内容分类：过程性文本（推理轨迹、冗余字段、调试信息）可丢弃；叙述性结论可摘要；数字与口径标签不可压缩只能句柄化；任务约束与全局状态常驻。压缩的判断标准只有一条：这句话后续被引用的概率——高概率常驻，低概率外置，零概率丢弃。误判代价不对称：把“会被引用的”丢了（报告出错），比把“不被引用的”留着（多花 token）严重得多——所以关键类宁可高估。",
 cj="现场：早期统一摘要压缩，一次评价后报告里“最大回撤”与正文叙述对不上——摘要时把回撤的“区间口径”摘掉了（回撤值不带区间就没有意义）。从此确立：口径标签与数字绑定为原子单位，不可分割、不可摘要。",
 fa=[("统一摘要压缩全部历史", "实现简单", "数字失真、口径丢失——金融场景致命", "否"),
     ("top-k 截断（留分数最高的）", "简单可控", "分数不等于后续需要；关键约束可能被截", "否"),
     ("分类压缩 + 预算装箱：不可压类句柄化、可摘类摘要、按节点预算分配", "失真风险结构性消除；预算可控", "分类规则与预算分配需要持续调优", "采用")],
 cq="采用：内容四分类（不可压 / 可摘 / 可弃 / 常驻）+ 分节预算（每节 token 上限，全局约束与数字句柄优先装箱）。踩坑一：预算平均分配导致关键分析节 starving——预算按节点重要性加权（结论节 > 分析节 > 过程节）。踩坑二：摘要丢“冷门但关键”的反例信息（某指标的异常值）——反例类内容加权保留（异常值是分析的金子不是噪音）。踩坑三：压缩后的上下文模型“找不到”句柄内容——句柄进上下文时带语义描述与典型值，模型能判断“要不要拉取”。与维度 7 的黑板共用一套句柄体系。",
 yz="压缩前后关键信息保留率（数字与口径全量保留是硬约束，叙述类人工评估）；下游报告质量 A/B（压缩策略 vs 全量基线在短任务上对比）无显著差异；单任务 token 成本下降约五成【占位：__%】。",
 wj=[("为什么不用长上下文模型一劳永逸？", "窗口大不等于注意力好——长上下文的中间信息利用率实证下降（lost-in-the-middle），而绩效评价恰恰是“中间的数字最重要”。且成本随窗口线性涨，分级压缩是结构解。"),
     ("压缩规则怎么来的？", "从错误反推：每一次“引用错对象”或“口径丢失”的 badcase 都归因到分类规则，修正规则再回归。压缩策略不是设计出来的一次性产物，是被 badcase 喂出来的运营资产。")],
 industry="CE 综述的压缩分类（压缩什么/何时/如何）与 Manus 的分层路线是理论坐标；我们的贡献是把“不可压类”精确到金融语义（数字+口径原子绑定）。",
 insight="压缩的判断标准只有一个：这句话被引用的概率——而金融语义决定了“数字+口径”的被引用概率永远是 100%。"),

dict(no=9, title="成本工程：模型分级与 token 预算",
 value="DeepResearch 形态的单次任务 token 消耗是对话的几十倍——不做分级就是烧钱；而绩效评价的环节错误代价高度不对称，分级有明确的依据。",
 qm="量化背景：全流程用旗舰模型，单次评价成本是普通问答的约 40 倍【占位：__】；直接全线换成便宜模型，规划质量崩塌（拆解错分析框架，全盘返工）。问题的正确形态不是“用哪个模型”而是“哪个环节用哪个模型”——分级的依据是错误代价：规划错 = 全部返工（最高代价），参数抽取错 = 单步重试（低代价），格式整理错 = 无伤大雅。",
 fx="按认知密度给环节分层：规划与勾稽校验是“高认知密度”环节（错误代价高、需要领域推理）——强模型；参数准备、结果格式化是“低认知密度”环节——轻模型；中间还有一档“中认知”（归因解读、结论组织）——中档模型。这个分层必须显式建模成配置而不是写死——因为“什么算高认知”随模型价格和能力变化。",
 cj="现场：参数抽取环节（把“近三年”转成时间窗参数）用旗舰模型，单步 800 token、延迟 2s；换轻模型后单步 120 token、延迟 300ms，准确率几乎无损（这是模式化转换任务）。反过来试过把规划也换成轻模型——分析框架拆解错误率明显上升，一次拆错的返工成本抵消了十次省钱。",
 fa=[("全线旗舰模型", "质量上限最高", "成本不可持续；低价值环节烧高价值模型", "否"),
     ("全线轻模型", "成本最优", "规划与校验环节质量崩塌，返工抵消节省", "否"),
     ("按认知密度三级分级 + 全局预算", "成本与质量的帕累托前沿；错误代价对齐", "路由配置要维护；级别边界要校准", "采用")],
 cq="采用三级：规划 / 勾稽校验 = 强模型；参数抽取 / 格式整理 = 轻模型；归因解读 / 结论组织 = 中档模型。全局 token 预算上限，超预算触发降级策略（减分析深度 / 减对比维度，缺口显式声明）。踩坑一：轻模型做参数抽取时枚举幻觉（编造组合 ID）——枚举动态候选机制结构性防御（见维度 3），与模型分级正交。踩坑二：轻模型摘要丢数字——数字类内容强制走强模型或不摘要（数字不可压缩原则再次生效）。踩坑三：分级配置写死在代码里，模型价格变动后失效——分级路由做成配置（环节→模型档位），价格与能力变化只改配置。与计费系统联动：模型分级直接改变 API 倍数成本结构，降本成果传导到客户账单。",
 yz="分级前后质量 A/B（分析框架完整性、勾稽一致率无显著差异）+ 成本对比：单次任务成本下降约六成【占位：__%】；预算触发率监控（超预算降级的占比，过高说明定价或预算失衡）。迭代：环节-模型映射按“错误代价 × 单价”每季度重评。",
 wj=[("分级的依据是什么，不会拍脑袋吗？", "错误代价不对称性：规划错的代价是全盘返工，抽取错的代价是单步重试——代价差一到两个数量级，模型单价差不到一个数量级，所以贵模型花在高代价环节是严格占优的。而且这个映射是配置，随价格与能力重评，不是一次性决策。"),
     ("和计费系统什么关系？", "直接关系：模型分级改变单次任务的真实成本结构，计费的 API 倍数要反映这个结构——内部降本和对外定价是同一套成本观测的两个视图。成本工程做不好，计费倍数就是拍脑袋。")],
 industry="多家产品采用“强模型规划 + 轻模型执行”的混合编排——成本结构是产品设计的一部分（Anthropic：token 用量解释 80% 成本方差）。",
 insight="成本工程的核心：把“贵的模型”花在错误代价最高的环节——分级依据是代价不对称，不是模型排行榜。"),

dict(no=10, title="评测体系与 RL 化演进：从编排到学习的路线图",
 value="编排式方案的天花板清晰可见：检索与调用策略靠人写规则，长尾场景覆盖不全。终局是 RL 训练（DeepResearcher 路线），而 RL 的前提是环境与奖励——也就是埋点与评测。",
 qm="现状的边界：skill 编排覆盖了高频分析框架（八成流量【占位：__%】），但长尾提问（表述多变、框架组合新颖）的处理质量 plateau——规则写不过来，写了的规则又互相打架。问题定义：如何从“编排的 plateau”走向“学习的扩展性”？分三步：编排积累数据 → 埋点评测升级为可验证奖励 → 小规模 RL 实验。",
 fx="RL 化的前置条件清单（对照 DeepResearcher / Search-R1 的配方）：环境（可交互的工具环境——我们已有：组件 API 网关就是环境）、动作空间（工具调用——已有）、奖励（事实正确性自动判定——缺！这正是要建的）。奖励设计的洞察：绩效评价的奖励比开放研究好做——数字勾稽一致率就是天然的 verifier 奖励（可自动计算、无歧义），分析完整性有 rubric——这是领域给 RL 的礼物。",
 cj="现场：用户对报告做了三处编辑——改了一个结论的措辞、修正了一个时间窗表述、删了一段重复分析。前两类是有信号的事实性修改（奖励正样本），第三类是风格修改（噪声）。原始采纳行为直接当奖励会引入大量噪声——需要过滤。",
 fa=[("继续堆编排规则", "零新基建", "边际收益递减，长尾永远盖不住", "否（作为基线保留）"),
     ("直接上 SFT 微调（拿好任务做监督数据）", "起步快", "只学形不学策：模仿轨迹不等于优化目标；坏轨迹混入难过滤", "暂缓"),
     ("三步走：编排积累 → 可验证奖励 → 小规模 RL（GRPO 系）", "扩展性的正解；奖励可自动计算", "基建投入大；奖励噪声要治理", "采用（分阶段）")],
 cq="已落地第一步、在建第二步：①全链路埋点——每次工具调用、参数、结果、勾稽输出、用户采纳行为（直接用 / 编辑后用 / 弃用）留痕；②奖励通道——数字勾稽一致率（verifier 奖励，自动）+ 分析完整性 rubric（模型评审，人工金标校准）。踩坑一：用户编辑行为混入风格修改噪声——只取事实性修改（数字、时间窗、口径的修正）作为奖励信号，风格修改过滤。踩坑二：采纳信号有偏（用户没编辑可能只是没细看）——弱信号降权处理，与勾稽奖励分层组合而不是等权平均。踩坑三：奖励 hacking 风险（策略学会讨好勾稽器而非真做对分析）——奖励组合里保留人工抽检通道做对抗验证。",
 yz="埋点覆盖率（任务全链路可回放）100%；奖励与人工判断一致率校准【占位：__%】达标后才进 RL 实验；RL 实验以编排基线为对照（完成率 / 勾稽率 / 长尾覆盖）。诚实边界：RL 化处于数据与奖励准备阶段，未生产化——面试如实陈述阶段与下一步。",
 wj=[("你们的 RL 化做到哪一步了？", "诚实版回答：环境与动作空间随系统天然就有，奖励通道在建——数字勾稽是天然的 verifier 奖励。这个准备本身就是工程量，而且它反过来提升了编排版的质量（评测更严了）。不上 RL 不是不懂 RL，是基建顺序的清醒。"),
     ("为什么绩效评价的 RL 比开放研究好做？", "奖励：数字勾稽可自动验证、无歧义；环境：组件 API 确定性强、成本低。开放研究的奖励要人评或弱模型评，我们要的是对账——领域结构决定了 RL 落地难度，这是我选这个场景做深的原因之一。")],
 industry="Search-R1 → DeepResearcher → Tongyi DeepResearch 是 RL 化 deep research 的演进三部曲——我们的三步走与这条线的工程节奏对齐。",
 insight="埋点不是运营工具，是未来 RL 的奖励基建——今天的数据结构决定明天能训什么。"),
]

DIM2_EXC = [
("组件/数据接口限流（429）","每端点独立令牌桶 + 指数退避重试；持续限流则切换降级路径或标注缺口。经济性权衡：降级是否启用备选计算路径由指标重要性决定，非核心指标直接标缺口。"),
("规划出循环依赖（节点 A 依赖 B，B 又依赖 A）","规划输出做 DAG 校验，发现环则合并或拆解重排；重排仍失败则回退到该 skill 的兜底线性流程。"),
("关键组件返回空/异常值（勾稽发现归因贡献不闭合）","不编造：该分析维度显式输出'计算异常，待复核'并降级——把失败转化为可见的质检需求，绝不用'看起来合理'的数字补位。"),
("并行部分失败导致分析不平衡","缺指标时报告自动降级为'已有指标 + 缺口说明'；用户体验上宁可短不可错。"),
("多组合对比时中间结果张冠李戴","黑板句柄带语义描述（节点+内容摘要），拉取前先验匹配；数字类中间结果锁定来源标签（组合 ID + 时点）。"),
("工具循环调用（同工具同参数反复调）","状态机防环：计数超 2 次强制跳出，触发节点级失败评估——局部重试还是升级为任务级重规划。"),
]

def dim_card(d, case_prefix):
    # 六步方法论渲染：定义→分析→场景→方案对比→采用与踩坑→验证与迭代 + 深挖预演
    fa_rows = ''.join(
        f'<tr><td><b>{n}</b></td><td>{p}</td><td>{c}</td><td style="white-space:nowrap">{v}</td></tr>'
        for n, p, c, v in d['fa'])
    wj_rows = ''.join(
        f'<div class="sstep"><span class="stag wj">追问 {i+1}</span><div><b style="color:#0f172a">Q：{q}</b><br><span style="color:#1f4e79">A：{a}</span></div></div>'
        for i, (q, a) in enumerate(d['wj']))
    steps_data = [
        ('① 问题定义', d['qm']), ('② 问题分析', d['fx']), ('③ 问题场景', d['cj']),
        ('④ 方案对比', '<table class=sel><tr><th style="width:150px">方案</th><th>优势</th><th>劣势/风险</th><th style="width:60px">结论</th></tr>' + fa_rows + '</table>'),
        ('⑤ 采用与踩坑', d['cq']), ('⑥ 验证与迭代', d['yz']),
        ('业界对比', d['industry']), ('设计巧思', d['insight']),
    ]
    steps = ''.join(f'<div class="sstep"><span class="stag">{lab}</span><div>{txt}</div></div>' for lab, txt in steps_data)
    wj_block = f'<h4 style="margin:12px 0 4px;color:#b91c1c">🔥 面试官深挖预演</h4><div class="strug" style="border-left:3px solid #dc2626">{wj_rows}</div>'
    return f"""<div class="tot">
<div class="tot-head"><h3>维度 {d['no']} · {d['title']}</h3><span class="sub">{case_prefix}</span></div>
<div class="box ideab" style="margin:8px 0"><b>价值点：</b>{d['value']}</div>
<div class="strug">{steps}</div>
{wj_block}
</div>"""
